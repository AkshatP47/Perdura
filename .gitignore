# Dependencies
node_modules/
.pnp
.pnp.*

# Build output
.next/
out/
build/
dist/
.open-next/
.wrangler/

# Environment
.env
.env.*
!.env.example
.dev.vars

# Test and coverage
coverage/
*.tsbuildinfo
next-env.d.ts

# Editor and OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# -----------------------------------------------------------------------------
# Excluded from any future public export
# -----------------------------------------------------------------------------
# reference/ is tracked in this private repository but must never appear in a
# public export, open-source release, published archive or Store submission
# bundle. It is competitor material kept for structure and information density
# only. The exclusion is enforced by `reference/ export-ignore` in
# .gitattributes, which drops it from `git archive` output. If you produce an
# export by any other means (a manual copy, a release script, a Docker context),
# exclude reference/ explicitly.
#
# Do not uncomment the line below unless you intend to untrack the directory:
# reference/
